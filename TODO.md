# TODO

只记录**尚未完成**的工作项。已完成能力的行为说明见 `README.md` 与 `docs/usage.md`；系统结构与设计决策见 `docs/architecture.md`；历史实现过程见 git log。

## 定位（2026-09-11 评审确立，一切取舍的基准）

**竞争对手是开源工具（SavvyCAN / BUSMASTER / candump+PlotJuggler / python-can+cantools），不是 CANoe。** 目标：比开源更流畅、更易用、功能更强，但保持轻。竞争四维度：**打开即分析 / 检索顺手 / 渲染不卡 / 零配置出结论**。CANoe 的"重"（面板 HMI、诊断栈、测试报告、多机分布式、实时与时间同步、多协议、用户权限）明确不做——那些不是更好的功能，它们就是"重"本身。既有优势不许丢：DBC 解码完整性、事实源纪律（DBC 唯一事实源 + 角色两态 + 驱动可插拔）、工程可复现（.rxproj + profiles + 无头 CLI）。逐项对照与依据见 `docs/roadmap.md`（§1-§2）。

**未立项的路线建议**（定位取舍、优先级排序、脚本语言决策）见 `docs/roadmap.md`——那是"建议 + 依据 + 待决策"，不入本文；采纳后拆成工作项并入相应 P 段。

## P0：单一事实源 + 角色模型（2026-09-08 立项，2026-09-09 六个切片全部落地 ✅）

**立项背景**：CANoe 的病根是"网络事实存了两份"——DBC 一份、仿真配置一份，"以谁为准"从设计上悬空，手动同步、改库忘加节点、restbus 漏配全是衍生物。三类现有软件的对比：分析工具（SavvyCAN）用"无节点"回避问题但做不了仿真；CANoe/BUSMASTER 把节点归工程文件，付出一致性代价；AUTOSAR/codegen 流让节点归拓扑描述，一致性由生成保证。**我们的定位：SavvyCAN 式的开放 + AUTOSAR 式的事实源纪律**——市面上没有工具把这条路走通。

**模型**：三个正交概念分开——

| 概念 | 回答的问题 | 事实来源 |
|------|-----------|---------|
| 总线拓扑 | 总线上有哪些节点、发什么报文 | **DBC 唯一事实源**，程序从 DBC 推断，绝不重复声明 |
| 执行角色 | 每个节点由谁扮演 | 每节点一行的角色声明，按 Profile 覆盖 |
| 硬件映射 | 接到哪块卡哪个通道 | 独立 overlay（阶段 5），不入项目本体 |

"DBC 节点 ≠ 仿真节点"的不一致由此在模型层面消灭：角色只能声明在 DBC 已有的节点上，错配报错而非静默。**上一版"GenNode 自由命名逻辑节点"方案作废**——自由命名会复活第二份节点清单。

### 已就绪的地基

- ~~多数据库挂接~~ ✅：一条总线合并挂多份 DBC，角色校验对照合并后的全部挂接库。
- ~~CLI 一等公民~~ ✅：`--replay` / `--project` / `--stats` / `--check-script` / `--profile`。
- ~~虚拟时钟与真实时钟同接口~~ ✅：CoreDrive 双驱动，确定性仿真与无头 CI 同一条代码路径。
- ~~本地状态不入工程~~ ✅：窗口布局/草稿/运行计数在 `roxy-can.json` 与会话态，工程 JSON 只存定义。
- ~~路径相对化~~ ✅：DBC 路径相对工程目录。
- ~~文本格式、schema 容错~~ ✅：serde 缺省 + 未知码丢条约定；`schema_version` 已落（现值 1，旧文件读作 0）。

### 落地切片（依赖排序）

1. ~~**角色模型核心**~~ ✅：`NodeRole` 三态——`Simulated`（模拟）/ `Monitor`（监听，只收不发）/ `Absent`（离线，默认态：未声明即离线）。GroupCardView 变**角色卡片**：全部 DBC 节点可见（不只发送节点），三态下拉切换；Network 视图同源（节点框色条区分模拟/监听）。工程 JSON 存 `node_roles`，旧工程的 `sim_nodes` 列表迁移为 Simulated，未知角色名丢条。
2. ~~**restbus 语义**~~ ✅：`absent` 不产生仿真负载（未声明即离线，restbus 无需配置）；`monitor` 只收不发；角色切换即时生效。（原"切入模拟加条目并发车"已随 P4 收紧为**角色 = 总开关**：切入模拟只放行闸门，条目是否发车仍看逐条开关——见 P4。）
3. ~~**符号引用校验**~~ ✅：脚本 `sig()` / `set_sig()` 引用的信号名在 Apply（节点装配层）对照 DBC 检查，未知名进日志/报错——拼错信号名是显式失败，不是运行时静默。保持红线：校验在装配层，语言内核仍零总线假设。（候选立项 R2 把它扩展为发送集 / 写入集 / 响应映射的完整静态校验。）
4. ~~**Profile 档案**~~ ✅（角色覆盖部分）：`profiles/<名>.toml` 角色覆盖 + CLI `--profile <名>`（需配合 `--project`，经 `<工程目录>/profiles/` 解析）；覆盖优先于工程保存的条目状态，未知总线/节点/角色词整份拒绝；解析走 `toml_edit`（依赖树已有，只增不改锁语义）。硬件映射 overlay 待阶段 5，GUI 选择器待有真实需要。
5. ~~**DBC 变更自动检测**~~ ✅：运行中校验和轮询，外部修改自动刷新（消灭"改库忘重载"）；重载后为 Simulated 节点补建库里新增报文的生成器条目（不自动激活——"不发车"红线同样适用于重载）。
6. ~~**格式收尾**~~ ✅：工程 JSON 加 `schema_version`（现值 1，旧文件读作 0，迁移以此为分支依据）；`.gitignore` 明确本地状态与运行产物边界（`roxy-can.*` 状态文件 / `record_*.asc` 录制默认落点）。

### 明确不作

- 自由命名、与 DBC 无关的逻辑节点（上一版 GenNode 方案）作废：角色只能挂在 DBC 已有节点上，防"第二份节点清单"复活。
- 图形化拓扑编辑器：继续不做，实体表（见节点体系）足够。

## P1：界面层次重构（2026-09-09 上午首轮 GUI 验收反馈，2026-09-09 当日落地 ✅）

用户实测截图逐条反馈，全部落地：

1. ~~**角色三态语义不明**~~ ✅：`NodeRole::hint()` 三句说明——模拟=本工具以它名义发车；监听=在总线上只收不发（接真实硬件后流量来自真实节点）；离线=不在仿真总线（报文来自回放块或硬件）。Network 详情选择器下方常显当前角色的说明，Entities 角色下拉悬停出解释。
2. ~~**生成器按节点分组**~~ ✅：条目按（总线 → 节点）折叠分组，组头 = 组名 + 角色徽标 + "N/M 条发送中"，组级"开/关"一键翻转整组；未归属节点的条目进"(未分配)"组；搜索时自动平铺。
3. ~~**角色与条目状态可打架**~~ ✅：组头如实显示"N/M 条发送中"——模拟节点全关条目一目了然；组级开关是合法的手动覆盖，角色切换仍是权威同步。
4. ~~**Nodes 与 Entities 重复、脚本编辑器全堆一窗**~~ ✅：**Nodes 窗口已删除**。每个脚本节点的编辑器是独立窗口（"脚本 · 节点名"，可多开，随节点删除/工程替换自动关闭），入口：Entities 行点击、Entities 顶部"+ 脚本节点"、Network 节点详情的本总线脚本列表（带 ●○○ 运行态）与"+ 脚本节点"。
5. ~~**波特率步进输入**~~ ✅：Buses 窗口 arb/FD data 改为直接输入 kbit/s 数值的文本框（失焦提交、非法回退、钳位 ≥1）。
6. **CSV 口径细化**：降级——等用户明确提出诉求再做，不主动排期。

### 需要人工验收（P1 完成后）

- 三态角色的说明文案是否解惑（监听 vs 离线）：Network 详情选择器下方、Entities 下拉悬停、生成器组头悬停。
- 生成器按节点分组后，"模拟某节点"是否一目了然；模拟节点全关条目时"N/0 发送中"是否显眼；组级开/关是否顺手。
- 砍掉 Nodes 窗口后：新建脚本节点（Entities 顶部按钮）→ 编辑器自动弹出 → Apply → 运行 的链路是否比原来顺手；Entities 行点击脚本节点是否正确弹出对应编辑器。
- 波特率直接输入（输错回退原值）是否顺手。

## P2：Kvaser 硬件联动（2026-09-09 深夜指令，✅ 第一版落地）

本机已装 Kvaser 驱动（canlib32.dll 在 System32）。目标不是自动判断，而是**手动机制**：用户逐节点决定"模拟它的报文是否经 Kvaser 发上真实总线"。

模型（对应 P0 的硬件映射 overlay）：

- **RX**：挂接的 Kvaser 通道收到的一切帧照常进总线（聚合/Trace/规格/触发都看得见）——真实节点的流量由此而来。仅仿真模式生效。
- **TX**：逐节点"经硬件发送"开关（默认关）。开启后，该节点名下发生器条目的帧照常进内部总线（视图可见）**并同时** canWrite 上真实总线；关闭则只进虚拟总线。真实节点已存在时，把角色设为**监听**（我们的流量不上线）而不是模拟，即可避免双发——这是用户的显式选择，程序不做判断。
- ~~脚本节点 / 触发反应帧 v1 不上硬件~~（已随 P4 解除：逐节点"经硬件"开关同样管脚本 `send` 与反应帧）。

落地情况：✅ canlib32 运行时加载（LoadLibrary + GetProcAddress，零构建依赖，驱动缺失优雅降级）；✅ 安全封装（枚举、open、波特率预设、BusOn/Off、write、非阻塞 read、close/Drop；消息标志位值经回环实证修正）；✅ 核心集成（挂接表、RX poll、TX 出口开关、命令、快照视图）；✅ UI（Buses"硬件"列下拉挂接/解挂、生成器组头/节点窗口/脚本编辑器"经硬件"开关）；✅ 测试（mock 端口测门控/RX/解挂清开关）；✅ 回环实证（虚拟通道对互相收发、RTR/STD 标志解码正确）。硬件映射为会话状态。

余量：多适配器同总线多句柄。物理适配器只收句柄写入静默失败（见 usage.md 硬件节）。~~硬件映射入 Profile 持久化~~ ✅（2026-09-11）：`profiles/<名>.toml` 增 `[[hw]]` 节（bus + channel），波特率取总线自身设置；**全量声明**——未列出的总线应用档案时解挂；GUI Profile 行与 CLI `--profile` 同源；校验 all-or-nothing，硬件运行时打不开进状态行不中止。

**FD 上硬件（2026-09-11 落地 ✅）**：查证 Kvaser 官方 canstat.h 发现并修正标志位错位——FD 标志住在高位字节（`canFDMSG_FDF=0x010000` / BRS / ESI），旧代码 TX 用 0x0100、RX 用 0x0080 判 FD（实为 canMSG_TXRQ），FD 帧会被完全误标。修复后：挂接按总线 FD 数据段波特率申请 FD 能力（`canOPEN_CAN_FD` + `canFD_BITRATE_*` 预设 500k/1M/2M/4M/8M，无预设/硬件不支持降级经典并如实标注）；FD 帧 TX 带 FDF/BRS 标志、RX 解码 FD/BRS/ESI；Buses 行与状态行显示 FD 态。虚拟通道回环实证：经典 5/5、FD 5/5、RTR 误判 0。

## P3：节点中心化（2026-09-09 深夜指令，随 P4 全部落地 ✅）

- ~~**每节点独立生成器窗口**~~ ✅（P4 切片 3，2026-09-11 按用户指令改为 **Network 节点详情内嵌**）："窗"按钮与独立节点窗口已删除——节点生成器（条目 + Add + 响应规则 + 经硬件）与脚本列表一起嵌入 Network 节点详情，节点就是编辑单元；总览窗口只留**未分配条目** + 逐总线批量开关 + 按 hex id 添加。（原"总览保留分组总览"的裁定作废：用户明确节点为中心、条目不挤在一起。）
- ~~**触发响应入节点**~~ ✅（P4 切片 3）：Send 类触发规则列在节点窗口底部，一键跳转 Triggers 窗口管理。
- ~~每节点三种发车方式聚合一处~~ ✅（P4 切片 3）：生成器（周期队列）+ 触发反应（响应帧）在节点窗口；脚本为绑定该节点的独立编辑器窗口（Network 详情就近列出）。

## P4：角色总开关语义 + 效率 + Profile GUI（2026-09-10 界面与语义重构计划，五切片全部落地 ✅）

**根因**：旧模型里"模拟"角色会强制打开该节点全部条目、退出强制全关——角色切换覆盖了用户对每个信号的自定义开/关。正确模型（已落地）：

- **角色 = 总开关**：模拟 = 允许该节点发车；监听 = 只收不发；离线 = 不在仿真总线。
- **条目开关 = 自定义**：每个报文条目的 On 开关持久保存，角色切换**永不改写**；实际发车 = 测量运行 && 角色==模拟 && 条目开 && 周期非 0；未分配条目不受角色闸。
- **新建条目默认关**：DBC 加载、重载补建、手动 Add 一律 inactive。
- **闸门锚定**：切入模拟时把该节点已启用条目的排程锚定到当前时钟（防陈旧排程突发）。

落地切片：

1. ~~**角色闸门语义**~~ ✅：发射循环与 next_deadline 加闸过滤；组头 "N/M 发送中 / N/M 条启用 · 总关"；条目 chip 第四态（条目开但角色闸关 → 灰蓝"总关"）。
2. ~~**发射时计算**~~ ✅：TxMsg 存最近一次实际发射的载荷（发射 slot / Send now / 触发 Send 三处同步更新），快照读存储值——显示随消息周期跳变，CPU 不再逐帧全量重算；从未发过的条目显示基准 hex。
3. ~~**节点中心界面**~~ ✅：脚本绑定 DBC 节点（`attached` 随工程保存），绑定脚本发帧受所属节点角色闸；脚本编辑器独立窗口（改名/换总线/经硬件开关）；节点生成器面板嵌入 Network 详情（2026-09-11 修订）；总览只留未分配条目。
4. ~~**Profile GUI**~~ ✅：Network 窗口顶部 Profile 行——下拉枚举工程目录 `profiles/*.toml` + 应用按钮（all-or-nothing，错误进状态行），语义与 CLI `--profile` 同源。
5. ~~**测试/文档**~~ ✅：闸门四态、角色不改写开关、绑定脚本受闸、快照读存储等用例；usage.md / README / TODO 同步。

## 节点体系（并入 P0 角色模型）

角色是节点的**角色**，节点还需要**驱动**——驱动形态继续可插拔：

- ~~GeneratorGroup 卡片~~ ✅ 已被**角色卡片**取代（P0 切片 1：全部 DBC 节点 + 三态角色）。
- ~~**ReplayBlock 驱动**~~ ✅ 第一版：块 =（名称 + 总线 + 日志 + 过滤器[DBC 发送节点 / id 列表]），仿真时钟下按录制间距把过滤后的流量注回总线，仅仿真模式生效；Replay Blocks 窗口编辑，声明随工程保存。**2026-09-11 按用户指令：块依附于节点**——从 Network 节点详情"+ 回放块"新建即绑定（默认只回放该节点报文），树里作为节点下 `[R]` 叶，绑定随工程保存；顶层"+ Block"仍是自由块；与全局 Replay 模式无关（仅仿真模式发车）。待议：块级倍速 / 起始偏移 / 按记录通道过滤。
- ~~**网络实体表（outliner）**~~ ✅ 第一版 → **2026-09-11 按用户指令删除**：Entities 扁平表被 Network 树取代——DBC 节点为叶（角色色标），绑定脚本作为下一层树叶挂在节点下（`*`/`o`/`.`/`!` 状态点，点击开编辑器），自由脚本挂总线根；角色与运行开关归节点详情与脚本编辑器。View 菜单项与桌面持久化字段一并移除。
- **测试模块**（远期）：带判定结果的自动化测试单元。
- 驱动与角色的关系：角色回答"这个节点现在谁扮演"，驱动回答"扮演时用什么跑"——一个节点同一时刻一个角色、一个驱动；切换角色不销毁驱动。

## 其他待办（并行小项）

### 观测与触发

- **State Tracker 收尾**：~~右键把 Data/Graphics 窗口里的信号直接加入跟踪~~ ✅（信号列表行右键 → 选 tracker 或新建并加入）；状态区段表的 CSV 导出口径细化（口径已与绘制共用 compute_segs；进一步细化需先明确用户诉求）。
- ~~**触发数值常量 UI 化**~~ ✅：预触发 / post-roll / 标记上限进入 Triggers 窗口顶部，随工程保存（默认即原常量 256/32/512，核心侧钳位）。

### 结构

- ~~**Trace 落盘**（#9）~~ ✅ 全部落地：~~录制按过滤只落一部分~~ ✅（工具栏 id filter）；~~静默丢头部~~ ✅（Trace 窗口提示）；~~容量可调~~ ✅（Measurement Setup 下拉）；~~磁盘支撑~~ ✅（2026-09-09）——热环溢出的帧追加写入临时归档文件（定长记录、核心线程整批写、每进程独立文件），**Export Trace 先回放归档再写热环**，导出覆盖整段运行；Trace 窗口显示"已归档 N 帧（导出包含）"，归档创建失败则降级为裁剪并如实提示；新运行截断归档。~~归档文件随 App 退出留在临时目录~~ ✅（2026-09-11：SpillFile Drop 删除，异常退出的残留靠系统清理）；"归档常驻可回看"需要 UI 再立项。
- ~~**派生信号**~~ ✅ 第一版（#11）：`emit_value("名", 表达式)` 内建——脚本即求值器（`sig()` 读数 + 数学/波形内建自由组合），发布为合成订阅流（键带 `EMITTED_ID_BASE` 前缀，绝不与真实报文撞键）；信号选择树"派生信号"分组，Graphics / Data / State Tracker 像选数据库信号一样选用。待议：跨总线引用、派生信号的单位/类型元数据。
- **双类展示层四元组化**：网络视图、TX 选择器、报文过滤器对同值双类只列一条（标准优先）。等有真实双类库需求再做。
- **动态库加载外部仿真元件**：进程内注册表已就绪（`script::register_extern`），动态库入口另议。
- ~~**BLF→ASC 转存**~~ ✅：`--convert <in> <out>` 纯流式转码（不经总线/时钟/录制器，旧"回放丢录制状态"语义不构成阻碍）。

### 需要人工验收（无 UI 自动化测试床）

- **总线核心线程化**：拖窗口、开模态、卡鼠标时波形不断流。
- **历史数据共享与性能**：多曲线 + Trace 1000 行 + 多窗口下，CPU 占用不高于单线程版。
- **v0.10.0 仿真节点全链**：Nodes 窗口、五个示例脚本、触发新动作（标记/清 Trace）、Graphics 标记竖线、State Tracker 双模式。
- **2026-09-09 角色模型 + 实体表 + 回放块 + 派生信号**：
  - Nodes 角色卡片与 Network 视图的三态下拉（模拟/监听/离线），节点框色条随角色变化；切角色即时停发/发车，退出模拟后激励不丢。
  - View → Entities 扁平实体表：徽标着色、行内开关与 Network/Nodes 联动、行点击跳转对应窗口、行右键插入/删除。
  - View → Replay Blocks：新建块 → 选 ASC → 启用后测量中按录制间距发车；坏路径出状态行报错；回放模式下块静默。
  - 信号选择树"派生信号"分组：`emit_value` 脚本发布后能进 Graphics 画曲线、Data 看数值。
  - 工具栏录制行 "id filter"：只录白名单 id；Measurement Setup 容量下拉与 Trace 窗口"head trimmed"提示。
  - CLI `--profile <名>`：错配报整份拒绝；旧 `.rxproj`（含 `sim_nodes`）打开后节点仍为模拟。
- **2026-09-10 P1 界面重构 + Kvaser**：
  - 角色下拉与说明文案；生成器按节点分组（组头 N/M 发送中、组级开/关、"窗"按钮、经硬件开关）。
  - Nodes 窗口已删除：Entities 行点击脚本节点弹独立编辑器（改名/换总线/经硬件开关在其中）；Network 详情的本总线脚本列表与"+ 脚本节点"。
  - Buses 波特率直接输入；"硬件"列挂接/解挂/刷新枚举。
  - Export Trace 含归档帧；Trace 窗口"已归档 N 帧"提示。
  - 真机 Kvaser：插上适配器后 `--kvaser-probe` 列通道 → Buses 挂接 → 监听真实流量 → 节点"经硬件"发车上线路。
- **2026-09-10 P4 角色总开关 + 节点窗口 + Profile GUI**：
  - 角色切换不再改写条目开关：切入"模拟"后条目保持原状（默认关的新条目不动），组头显示"N/M 发送中 / N/M 条启用"；角色非模拟但条目开着时出灰蓝"总关"chip。
  - 生成器行内 hex / 信号值随消息周期跳变（最近一次实际发射），从未发过的条目显示基准 hex；Send now 立即刷新。
  - 节点窗口（组头"窗"按钮）：只列本节点条目 + Add 限本节点报文 + 响应规则区；总览保留分组视图。
  - 脚本绑定：Network 详情"+ 脚本节点"建的脚本出现在该节点详情列表；所属节点切"离线/监听"后脚本停发，切回"模拟"恢复；编辑器里"经硬件"开关出现且生效。
  - Network 顶部 Profile 行：下拉枚举 profiles/*.toml、应用后角色立即生效；错配 profile 应用后状态行报整份拒绝原因。
- **2026-09-11 FD 上硬件 + Profile 硬件映射**：
  - FD 帧上硬件：挂接申请 FD 能力（Buses 行与状态行显示 FD 态），FD 帧经"经硬件"开关带 FDF/BRS 上线，收到的 FD/BRS/ESI 徽标正常。
  - Profile `[[hw]]`：档案声明"总线 → Kvaser 通道"，应用档案即挂接（含 GUI Profile 行）；未列出总线解挂；错配整份拒绝；旧档案（无 [[hw]]）不受影响。
  - 真机验收：`--profile bench` 一条命令完成角色切换 + 通道挂接；虚拟通道 FD 回环已实证（经典 5/5、FD 5/5）。
- **2026-09-11 GUI 验收反馈**：
  - ~~脚本还有旧的总线选择界面、自由脚本不挂节点~~ ✅：**自由脚本从模型中删除**——脚本一律依附一个 DBC 节点；编辑器头部只读显示 `节点 X · 总线 Y`（旧的总线选择下拉删除，SetNodeChannel 命令随之删除）；旧工程的未绑定脚本在库加载/工程恢复时**自动收养**到该总线第一个库节点下（注意：收养后受该节点角色闸，需把节点切"模拟"才会发车）。
  - ~~Entities 实体表窗口~~ ✅ 删除：脚本挂到 Network 树的节点下（脚本叶），节点设置归 Network 详情；View 菜单项与桌面/工程持久化字段移除，旧工程里的相关字段按"丢字段不丢工程"容错。
  - ~~Trace / Messages 的 Clear 不工作~~ ✅：两个按钮原来只重置窗口的过滤条件，不清内容——语义接错了。改为真清空：Trace 的 Clear 清显示与归档、Messages 的 Clear 清逐报文计数（与 Statistics 同源），测量照常进行，过滤器设置不再顺带重置。其余窗口的 Clear（规格 / id filter / 搜索框 / State Tracker 规则）核对过语义无误。
  - ~~节点生成器窗口不过滤条目~~ ✅："生成器 · 总线 · 节点"里把所有总线的条目全列了出来——行循环漏了按节点过滤（Add 下拉过滤了，行没有）。随后按用户指令**整体重构**：节点独立窗口删除，节点生成器（条目/Add/响应规则/经硬件）嵌入 Network 节点详情，总览只留未分配条目 + 按 hex id 添加。

## 2026-09-15 批次：角色两态化 + 硬件出口归总线模式（用户夜间队列 ✅）

1. ~~**角色两态化**~~ ✅：`NodeRole` 由三态简化为两态——**模拟 / 离线**（CANoe 同款）。`Monitor`（监听）与 `Absent` 行为完全相同（都是"我们不冒充它发车"），声明差异没有可感知语义，删除；真实节点的流量在 Real bus 模式下经挂接通道自然进入。旧工程/旧档案里的 `Monitor` 词按既有约定丢条/整份拒绝，行为上等价迁移为离线。
2. ~~**删逐节点"经硬件"开关**~~ ✅：CANoe 只有一个 simulated bus / real bus 总开关，逐模块开关是复杂度不是能力。删除三处 UI（生成器组头/节点详情/脚本编辑器）与 `SetNodeHardwareTx` 命令、快照 `hw_tx_nodes`；`Hardware::write_if_directed(bus, node, ..)` 改为 `write_if_live(bus, ..)`——Real bus 下"模拟"节点的**一切**发车（生成器条目、脚本 `send`、触发反应帧）直接上挂接通道；Simulated 模式照旧停发不丢配置。挂接本身（Buses 窗口）与 Profile `[[hw]]` 不变。
3. ~~**信号行拖柄**~~ ✅（同批 ①）：Data/Graphics/State Tracker 信号行重排为 `[::拖柄] [对号] [色块] 名字`——对号贴近色块、色号顺序符合直觉，拖拽改由行首 `::` 拖柄承担（拖柄是 2×3 圆点的隐形按钮），对号只管显隐。回归测试双向锁定：拖柄必须能换序，对号列必须不能换序（原地点击必须能切换）。
4. ~~**$信号 / @sysvar 速记**~~ ✅：CANoe 风格访问语法糖——读信号 `$报文名::信号名`（等价 `sig(id, "名字")`，id 由库按报文名解析，启动检查对名字做 DBC 核对，拼错显式报出）；读/写系统变量 `@sysvar::ns::name` / `@sysvar::ns::name = v;`（等价 `sys_get` / `sys_set`）。名字全是字面量——R2 可静态推导红线不破；语言内核零总线假设不破（报文名→id 的映射由节点启动时注入，VM 只见纯数据）。信号无写速记（写走 `set_sig` 缓冲语义）。
5. ~~**Profile GUI**~~ ✅：Network 窗口 Profile 行从"只能选"扩为全管理——**存当前**（输入名字把现在的角色 + 硬件挂接快照写成新 profile；名字拒绝路径分隔符与点号，写不出 profiles 目录）、**编辑**（系统默认编辑器打开选中 .toml）、**删除**（两次点击确认）。`[[hw]]` 格式补 `driver` 可选字（`Kvaser`/`Vector`，缺省 Kvaser 兼容旧档案）——应用档案不再把 Vector 通道错挂成 Kvaser。

## FlexRay 立项（2026-09-15，用户有 VN7640 可验证；RX-only 先行）

定位提醒：只做**总线监听**（RX-only），不做解码/脚本发车——那是"重做 dbc.rs"量级的另立项（依据见 `docs/roadmap.md` §8.2）。验证路径：用户的 VN7640（FlexRay 口）+ 集群参数。

- ~~**FR-1 枚举分类**~~ ✅（2026-09-15）：`vector::enumerate()` 读取 `channelBusCapabilities`，`ChannelInfo` 带 `can` / `flexray` 标记；`enumerate_flexray()` 列出 FlexRay 口；CAN 挂接下拉**排除显式 FlexRay 通道**。实测坑：虚拟通道的能力位报 0——过滤采用保守策略（能力位为 0 视为 CAN，保持旧行为；只有显式 FR 位才排除）。`+58` 偏移与 FR 位的真机校准列入 FR-2。
- **FR-2 RX-only 接收**（下一切片，需 VN7640 插上 + 集群参数）：`XL_FLEXRAY_CLUSTER_CONFIG` FFI 先用 MSVC offsetof 探针校验 packed 布局（同 CAN 结构体先例——没有校验不上产品）；打开流程 xlOpenPort(FLEXRAY) → xlFlexRaySetConfiguration → xlActivateChannel → xlReceive 事件流；帧进 Trace/日志，按 `(通道, slot, cycle)` 显示原始帧 + hex 载荷，不做信号解码。**需要用户提供**：集群参数（.arxml 或参数表），或确认用 VN7640 demo 默认集群。
- **FR-3 Trace 展示**：帧键模型从 `(channel, id, ext)` 扩展为协议感知联合键的前置调研 + Trace 行的 slot/cycle 列。
- **FR-4（另立项）**：FlexRay 数据库/集群解析器（.arxml ≈ 重做 dbc.rs）、信号解码、周期/抖动统计、脚本发车——与 R2 静态分析的关系一并重新评估。

## 2026-09-13 夜间批次：System Variables + 脚本编辑器升级 + Write 窗口 + 定时精度（主体落地 ✅）

1. ~~**System Variables 管理器 v1**~~ ✅：命名空间变量（namespace/name/init/可选钳位界限/unit/comment），View > System Variables 管理窗口（表格 + CANoe 式新建/编辑对话框，行内改值），定义随工程保存；脚本 `sys_get("ns::name")` / `sys_set("ns::name", v)`（宿主输入注入 + 写入经总线钳位生效）；每个变量发布为 namespace 分组的可观测流（Data/Graphics 直接选用）；每次测量开始复位为 init 并重发布。**待议**：字符串型变量（现仅数值）、枚举型、`@sysvar::` 表达式语法。
2. ~~**脚本编辑器侧栏 tab 化**~~ ✅：函数（插入模板 + 本节点 DBC 报文/信号）/ 大纲（处理器逐行带行号，容忍半成品源码）/ 收发（R2 静态事实表，见上）/ SysVar（已定义变量一键插入读/写模板）。事实按草稿 hash 缓存，敲键才重算。
3. ~~**侧栏插入到光标处**~~ ✅（用户反馈修复）：源码控件以 CALLBACK_ALWAYS 上报光标/选区/视口滚动，插入落在光标处（有选区则替换），不再追加到文件尾。
4. ~~**脚本编辑器行号 + 空格点**~~ ✅：行号栏与文本视口滚动同步（绑定的 ImGui 1.89 multiline 不软换行，行 = 行，像素精确；滚动量经回调内读取内层子窗口滚动得到）；空格以浅色圆点叠加显示（无换行 ⇒ 字形位置可精确推算，含水平滚动）。~~**语法高亮**~~ ✅（2026-09-13 第二轮）：字符级分类（关键字/数字/字符串/`//`与`/* */`注释，镜像 lexer 形态）按草稿 hash 缓存，叠加半透明色带着色（InputText 无法重涂字形，叠色是可行形态）；编译错误行在行号栏红条 + 红号标出。**剩余**：若要"真"的逐字形着色需自绘编辑器控件（大件，另议）。
5. ~~**Write 窗口**~~ ✅：总线核心的整系统滚动日志——节点 print（`[节点]` 归属）、check/timer/sysvar 告警、错误醒目、命令状态作 info（带总线时钟戳）；未滚动到底时粘底；Clear 清空；按 kind 过滤（会话态）与**导出**（当前过滤视图 → 文本文件，2026-09-13 补齐）。
6. ~~**set_timer 定时不准**~~ ✅（根因修复，非仿真补丁）：脚本定时器本就是绝对死线 + 核心事件循环按死线唤醒，不准在**唤醒精度**——Windows 条件变量等待按系统定时器中断（默认 15.625 ms）解析，recv_timeout 每次最多晚一个节拍（200 ms 单次 → 实测 206~215 ms；10 ms 周期 → 每 15.6 ms 一发）。修复：核心线程生命周期内 `timeBeginPeriod(1)`（操作系统正规机制，Windows 10 2004+ 进程内生效）；顺带修周期定时器按"晚发时刻"重锚导致的**累计漂移**（现按计划时刻 due+period 重锚，长停机塌缩错过节拍）；`timer_dues` 不再上报已停槽位。

### 需要人工验收（2026-09-13 批次）

- System Variables：新建（含非法输入报错文本）、行内改值、编辑/改名/删除、随工程保存恢复；脚本 `sys_get`/`sys_set` 读写与界限钳位；未定义引用的 start 告警与写入丢弃告警；测量开始复位为 init；Data/Graphics 里选 namespace 分组的变量画曲线。
- 脚本编辑器：四个侧栏 tab 内容正确（大纲行号、收发 `*`/`!`/`-` 标注）；插入落在光标处与选区替换；行号随滚动同步、空格圆点对位（含水平滚动）；Apply 后"收发"与告警一致。
- Write 窗口：脚本 print 实时上屏、节点归属、粘底行为（滚上去读旧行不被拽回）、Clear。
- 定时精度：真机跑 `set_timer("resp", 200)` 响应帧，Trace 时间戳应在 200±2 ms 内；10 ms 周期定时器节拍均匀不再漂移。
- Trace/Messages 等全部表格窗口：表头冻结生效（滚动时标题行不动）。

## R1：离线分析优先（候选立项——依据与逐项对照见 `docs/roadmap.md` §2-§3）

现状：日志必须按墙钟把回放跑完才有数据，seek 跳过的帧不补进任何聚合；没有离线模式。这是与开源档差距最大的一块，而它同时放大全部现有能力（解码 / Graphics / Tracker / 规格都在等输入）。按此顺序：

1. ~~**日志全量装载 + 真离线模式**~~ ✅ 第一版（2026-09-11 夜间）：工具栏 Replay 区新增 **Scan** 按钮——一次性把整份日志灌进聚合 / 统计 / 负载历史 / 规格 / Trace（含归档），无回放时钟、无触发动作、无节点分发、不录制；扫完自动停靠（测量停止），Play 照常从头重放且重新计数（无双算）。重扫从零重测（幂等）。语义决策（roadmap §6.2/6.3 的自主裁定）：扫描 = "立即跑完整个回放的观测面"，与回放模式共用同一数据管线，不是并列通道。
2. ~~**Trace 虚拟滚动**~~ ✅（2026-09-11 夜间）：过滤后的行缓存 + ImGuiListClipper 按可视区渲染——1000 行上限拆除，2M 行环全量可滚可排（缓存 20 万行封顶，超出按最新优先截断并如实显示数量）；排序在缓存上原地执行。待白天可视化复验滚动与排序手感。
3. ~~**检索过滤补到 grep 档**~~ ✅（payload 字节搜索 / 帧类型过滤 / 信号值条件 / 时间范围跳转均落地）。~~书签与跳转时刻~~ ✅（Trace 行右键"跳转到此时刻"，回放模式下 seek 到该帧时间点）。仅剩：独立的时间范围双输入框（低优先级——现有跳转已覆盖核心需求）。
4. ~~**已算未上屏的计量**~~ ✅（2026-09-11 夜间）：Data 窗口补 Min / Avg / Max 列（订阅侧本就累积）；Bus Statistics 顶部新增每总线**负载历史曲线**（BusLoad::history() 的 60×100ms 桶，本就实现无人调用）；逐报文补**周期抖动 jitter**（对平均周期的绝对偏差 EMA）与 **Rx/Tx 分离计数**（Dir 列显示 Rx+Tx）。错误帧分类与控制器状态待接真机。

## R2：脚本静态分析（候选立项——依据与判定依据见 `docs/roadmap.md` §4）

**保留自研脚本语言，不引入 Lua / JS / Python**；投资从"补表达力"转向"补编译期静态分析"。结构上恰好可行：宿主函数表是编译期闭集、关键标识符全是字面量、id 来源封闭——效果集合可静态推导。

0. ~~**spike：静态发送集推导**~~ ✅（2026-09-11 夜间）：编译器在 handler 上下文中收集 `send` / `send_ext` 的静态事实——字面量与常量算术直接推导、`frame_id()` 由事件绑定约束、`on message *` 内记为通配转发、其余（变量 id）记 opaque。**examples 六例覆盖率 100%**（4 个发送全部可推导，0 opaque）——fail-closed 规则落地不会误伤现有用户脚本。
1. ~~**语言规则（fail-closed，先立）**~~ ✅（2026-09-11 夜间启用）：`send` / `send_ext` 第一参数必须静态可推导（字面量 / 静态全局的有界算术 / `frame_id()`），不可推导一律编译报错；`set_timer` / `emit_value` 名字同理。立此规则即永久拒绝"从外部来源长出 id"一类机制——这是定位的应有代价。
2. ~~**静态事实表 + 装配层校验**~~ ✅（2026-09-13 全部落地）：发送集对照 DBC 发送者声明（start 时 `[check]`）；反向漏实现（未发送的声明报文 → `[info]`，生成器代发注明、通配脚本豁免、条目封顶）；`set_sig` 字面量值对照 DBC 物理范围（`min==max` 视为未声明；顺带修复 set_sig 引用收集挂在 HOST_FNS 死分支的潜伏 bug）；接收集 + 系统变量访问集 + 响应映射进编辑器"收发"tab；**`--check-script --dbc <路径> --node <名>`** 把同一套检查（`dbc_checks()` 共用函数）跑进无头门禁，`[check]`/`[info]` 进报告、退出码仍只反映编译失败。
3. ~~**通配转发归类**~~ ✅（2026-09-13）：`on message *` 脚本在启动日志与 `--check-script --dbc` 报告中显式归类为「转发者/记录者」（发送集 = 接收集 ∩ 库声明）；通配 handler 内有发送（静态 id 或 `frame_id()` 转发）时追加自激成环提示（防护静态不可判定，故为 info 不是 check，指向 sniffer 示例的 `frame_id()` 自排除写法）；编辑器「收发」tab 同步标注。
4. **运行时兜底**：~~已裁定暂不需要~~——fail-closed 之后不存在动态 id 路径，越界只能来自 DBC 本身变更（有重载校验兜着）；如未来放宽 fail-closed 再重启此项。
5. ~~**表达力增量**~~ ✅（有界定长数组 2026-09-13 早些时候；位运算 `&` `|` `^` `<<` `>>`、复合赋值 `+=` 等全家族含缓冲元素形式、`switch/case/default`（各分支互斥无贯穿）2026-09-13）：位运算只收整数（浮点不提升）、移位越界报错、常量位算式折叠保持 `send(0x100 | 0x8)` 静态可推导；switch 在语法层脱糖为 if 链（调用主体经隐藏临时值恰好求值一次），编译器零改动。红线照旧：每次加特性先问"会不会让效果集合不可静态推导"。
6. **先做 spike**：拿 examples/ 六个示例测静态可推导覆盖率，再定抽象域大小。外部仿真元件（电池模型 / 诊断栈）走 `register_extern` 另议，与节点脚本两套机制不混用。

## 阶段 5：更多硬件源（适配器选型后启动）

适配器作为核心的又一个 `FrameSource`：RX 由驱动线程喂入，硬件时间戳锚定到核心时间轴；发生器 TX 由核心调度线程直接写适配器。硬件映射的 Profile overlay 已随 Kvaser 落地（`[[hw]]` 节），接入新适配器即复用。

- 分期：只收（2–4 天）→ 发送（约 1 周）→ CAN FD 与错误状态（数周）。
- 选型：CANable/candleLight（全 Rust 生态，无厂商 SDK 授权）或 PCAN-USB（PCanBasic.dll，行业最常见）。
- CI 替身：单通道 loopback 作为无硬件默认。

## Roadmap 候选（远期，按需启动）

- **轻量监控面板**：一个**纯文本**的监控窗口——用户自组的关键信息行（信号值、计数、状态文案），方便大屏/分屏时一眼看几个关键数。**明确不做** CANoe 式的完整 HMI 面板编辑器（一堆控件、图片定义状态机那种，太重——该项已入明确暂缓）；如果哪天真需要状态图形化，也先从"文本行 + 简单条件着色"这种最小形态试起。数据源与布局方案到时候按需求再定。
- **带判定的测试单元**：从 R2 的静态分析自然长出的门禁能力（"发送集符合 DBC 声明"作为一条断言）；要不要做成"跑完仿真再给结论"的测试单元，还是保持在 `--check-script` 编译期范围——待决策（`docs/roadmap.md` §6.5）。与节点体系里的"测试模块"远期项合并看待。

## 明确暂缓 / 边界外（当前定位下的否决——定位若变（如转向台架 / HIL），须显式重启决策，不许慢慢渗进来）

- **面板设计器 / HMI 控件**：CANoe 的"重"本身；需要 on key / 控件事件一整套动态事件模型。轻量纯文本监控面板保留为 Roadmap 候选（见上）。
- **换脚本语言底座（Lua / JS / Python）**：与"编译期可判定"正面冲突，得到的是表达力不变的阉割版 + 更多代码（依据见 `docs/roadmap.md` §4 与 R2）。
- **工业级测试报告（HTML / XML / PDF）**：与"零配置出结论"冲突；CSV + CLI 退出码已够。
- **多机分布式测量、实时系统与时间同步（PTP / GPS）**：定位外；核心是普通 std::thread。
- **用户 / 权限管理、国际化**：当前受众不构成瓶颈；中文字体与 IME 已解决实际痛点。
- **网关与跨总线路由模型**：依赖多数据库与节点体系先行，另立项。
- **诊断层（UDS / CDD / ODX / 刷写）**：按"更多协议"那一轮一起谈。
- **MF4 读取**：卡在"先拿到 Vector 真实导出样本"，没有样本不可验证。
- **LIN / FlexRay / 以太网**：按协议那一轮一起谈。

## 弯路记录（教训与纠正状态）

按影响排序。已纠正的保留条目作为回归防线；未纠正的指向对应待办。

1. **节点清单双源**（影响最大；✅ 已随 P0 角色模型纠正）：仿真节点名单存了两份——DBC 发送节点一份、`sim_nodes` 一份，"以谁为准"悬空。纠正：`NodeRole` 三态（模拟/监听/离线）取代 `sim_nodes`，角色只能声明在 DBC 已有节点上，旧工程列表迁移为 Simulated。
2. **帧类硬编码假设**（✅ 已纠正）：`build_node_inputs` 硬编码 `extended: false`——扩展报文的 `sig()` 永远读不到值；脚本 `send` 长缓冲无校验——能发出无 FD 标志的 64 字节坏帧。纠正：帧类进键模型、FD 自动取整 + 长度上限（fe3c331 之前的 955dac5）。
3. **脚本内建裸奔**（✅ 已纠正）：`ramp`/`sine_wave` 从已清空的栈取参（一调即崩）、`srand` 栈失衡——都是实现先落地、测试后补时暴露的。教训：内建落地即带测试，不留"先跑通再说"。
4. **跨层契约回归**（✅ 已纠正）：extern 解析链改造时，把"钩子拒绝调用 = 报错"静默改成了 nil。教训：改解析链先写契约测试钉住两条路径各自的语义。
5. **提交不完整**（✅ 已纠正）：一次提交漏掉 bus.rs，历史提交点编译不过。教训：提交前全量 `cargo build --tests`，而不是只验证改动的文件。
6. **测试写 CWD**（✅ 已纠正，翻车两次）：录制类测试不设路径 → 默认派生 CWD `record_*.asc`，并行运行同秒同名互删文件；修正后又出现漏网（rearm 测试）。教训：每个新录制测试显式 temp 路径，全量测试跑完检查 `git status`。
7. **PowerShell 批量改文件**（✅ 教训已记，但同日再犯一次）：批量重写文本引入 BOM，或以系统码页误读 UTF-8 中文注释导致乱码、甚至吞掉行首属性（`#[cfg(test)]` 被并入注释行）。教训重申：**批量文本修改只用 Edit 工具**；PowerShell 只允许 `[IO.File]::ReadAllBytes` 这类只读检查。
8. **裸 id 展示层的双类妥协**（已知妥协，未纠正）：选择器对同值双类只列一条、ImGui 控件 ID 需手工区分。等真实双类库需求。
9. **50k Trace 环静默丢帧**（未纠正 → 结构待办 #9）：长抓取丢头部。磁盘支撑方案见结构待办。
10. **UI 用了字体缺字形的符号**（✅ 已纠正，2026-09-11）：内嵌 Inconsolata + 系统中文字体的合并字体没有 ●○（U+25CF/U+25CB）等几何符号，Network 树里全部渲染成"？"；全角"·"和"→"在中文字体里有（→ 后仍换成了 `->` 更稳）。教训重申：**UI 字符串优先用 ASCII 能表达的东西**（色标用 `[S]`/`[M]`/`[-]`、状态点用 `*`/`o`/`.`），图形符号需要字体方案立项时一并解决。
11. **CanKingService 常驻占虚拟通道 init access**（✅ 已实证定位，2026-09-11）：CAN King 的后台服务 `CanKing`（自启动，GUI 关了也在跑）持有 Kvaser 虚拟通道的 init access，导致 roxy-can 收发挂接始终 canERR_NOTFOUND、降级只收。裸标志矩阵实证：该驱动 `ACCEPT_VIRTUAL(0x8000)` 一律 PARAM（不可用），唯一出路是停掉服务（管理员：`sc stop CanKing` + `sc config CanKing start= demand`）。注意虚拟通道的只收句柄其实仍能发车——"只收"标签在虚拟通道上不代表不能 TX。
12. **canGetChannelData 在本机必 AV；通道名走 canGetHandleData**（✅ 已实证，2026-09-11）：umbrella canlib32 的 canGetChannelData 在本机对任何 item（含文档内 item 13/26、256 字节缓冲、先 canLocateHardware）都 AV 杀进程。可行路径：**开一个 NO_INIT 句柄（不动总线参数不 BusOn）后用 canGetHandleData(item 13)** 读用户友好通道名（"Kvaser Leaf Light v2 #0 (Channel 0)"），item 码与通道级共用（canCHANNELDATA_*）。enumerate() 已改走此路径，下拉显示真实设备名。教训：FFI 探针必须单 item 单进程隔离，AV 会带走整个测试进程。
13. **Windows 默认定时器分辨率 15.625 ms 吃掉唤醒精度**（✅ 已纠正，2026-09-13）：核心事件循环按 `next_deadline` 算好等待时长，但 `recv_timeout` 在 Windows 上经系统定时器中断解析，默认 64 Hz——死线醒来必然晚 0~15.6 ms，脚本 `set_timer(200)` 实测 206~215 ms。修复用操作系统正规机制（`timeBeginPeriod(1)`，随核心线程生命周期持有），不做"多睡几次凑近"式补丁；周期定时器另需按计划时刻（而非晚发时刻）重锚，否则一次晚醒永久漂移后续所有节拍。教训：**"调度对、睡不准"先查 OS 计时器分辨率**，别急着怀疑自己的死线数学。
14. **imgui 模态草稿必须逐帧回写**（✅ 已纠正，2026-09-13）：变量/触发编辑弹窗每帧从 App 侧克隆草稿，控件键入只落在帧内克隆——ImGui 内部状态照常显示，但 Apply 校验的是陈旧副本（等于没改，且报错文本活不过当帧）。修复：每帧把克隆回写 App，成功产物与校验错误也走同一路径。教训：**imgui-rs 的 InputText 显示态和 Rust 侧缓冲是两份状态**，凡是"每帧 clone → 控件改 → 帧末读取"的形状都必须回写。
15. **PowerShell 改状态文件再犯**（✅ 已纠正，2026-09-13，教训 #7 重演）：清理测试残留时用 PowerShell `ConvertTo-Json` 重写自动保存文件——编码码页读入中文乱码、重序列化悄悄掏空数组，且第一次探测的 `blocks.Count` 结果本身就误导（单元素数组序列化形态）。发现后整份删除自动保存（可再生的会话态），用户工程文件未动。教训重申且加码：**状态文件要么用 Edit 工具、要么用程序的 serde 路径处理，PowerShell 只做只读探查；写回前必须先确认读到的形态正确。**

## 备注

- ~~UI 无 imgui 自动化测试床~~ 部分补齐（2026-09-13）：`src/ui_tests.rs` 无头冒烟床——imgui 不接渲染器也能逐帧跑真实绘制路径（构建字体图集、`ctx.render()` 收尾、全局上下文互斥串行），"全窗口齐开 / 脚本编辑器带高亮 / 编辑弹窗"三场景由测试守护，绘制层 panic 直接红测试。控件交互（点击、选区、输入法）仍靠人工验收，判定逻辑用无头测试自动证明。
- 唤醒精度远期可选：事件驱动核心的"最后 <1 ms 自旋收尾"开关（详见 `docs/architecture.md` 核心时钟模型）。
