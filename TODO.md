# TODO

只记录**尚未完成**的工作项。已完成能力的行为说明见 `README.md` 与 `docs/usage.md`；系统结构与设计决策见 `docs/architecture.md`；历史实现过程见 git log。

## P0：单一事实源 + 角色模型（2026-09-08 立项，最高优先级）

**立项背景**：CANoe 的病根是"网络事实存了两份"——DBC 一份、仿真配置一份，"以谁为准"从设计上悬空，手动同步、改库忘加节点、restbus 漏配全是衍生物。三类现有软件的对比：分析工具（SavvyCAN）用"无节点"回避问题但做不了仿真；CANoe/BUSMASTER 把节点归工程文件，付出一致性代价；AUTOSAR/codegen 流让节点归拓扑描述，一致性由生成保证。**我们的定位：SavvyCAN 式的开放 + AUTOSAR 式的事实源纪律**——市面上没有工具把这条路走通。

**模型**：三个正交概念分开——

| 概念 | 回答的问题 | 事实来源 |
|------|-----------|---------|
| 总线拓扑 | 总线上有哪些节点、发什么报文 | **DBC 唯一事实源**，程序从 DBC 推断，绝不重复声明 |
| 执行角色 | 每个节点由谁扮演 | 每节点一行的角色声明，按 Profile 覆盖 |
| 硬件映射 | 接到哪块卡哪个通道 | 独立 overlay（阶段 5），不入项目本体 |

"DBC 节点 ≠ 仿真节点"的不一致由此在模型层面消灭：角色只能声明在 DBC 已有的节点上，错配报错而非静默。**上一版"GenNode 自由命名逻辑节点"方案作废**——自由命名会复活第二份节点清单。

### 已就绪的地基

- ~~多数据库挂接~~ ✅：一条总线合并挂多份 DBC，角色校验对照合并后的全部挂接库。
- ~~CLI 一等公民~~ ✅：`--replay` / `--project` / `--stats` / `--check-script`；`--profile` 待接入。
- ~~虚拟时钟与真实时钟同接口~~ ✅：CoreDrive 双驱动，确定性仿真与无头 CI 同一条代码路径。
- ~~本地状态不入工程~~ ✅：窗口布局/草稿/运行计数在 `roxy-can.json` 与会话态，工程 JSON 只存定义。
- ~~路径相对化~~ ✅：DBC 路径相对工程目录。
- ~~文本格式、schema 容错~~ ✅：serde 缺省 + 未知码丢条约定；`schema_version` 字段待补。

### 落地切片（依赖排序）

1. ~~**角色模型核心**~~ ✅：`NodeRole` 三态——`Simulated`（模拟）/ `Monitor`（监听，只收不发）/ `Absent`（离线，默认态：未声明即离线）。GroupCardView 变**角色卡片**：全部 DBC 节点可见（不只发送节点），三态下拉切换；Network 视图同源（节点框色条区分模拟/监听）。工程 JSON 存 `node_roles`，旧工程的 `sim_nodes` 列表迁移为 Simulated，未知角色名丢条。
2. ~~**restbus 语义**~~ ✅：`absent` 不产生仿真负载（未声明即离线，restbus 无需配置）；`monitor` 只收不发；角色切换即时生效——切入模拟加条目并发车，退出模拟停发但条目/载荷/激励原样保留。
3. ~~**符号引用校验**~~ ✅：脚本 `sig()` / `set_sig()` 引用的信号名在 Apply（节点装配层）对照 DBC 检查，未知名进日志/报错——拼错信号名是显式失败，不是运行时静默。保持红线：校验在装配层，语言内核仍零总线假设。
4. **Profile 档案**：`profiles/*.toml`——角色覆盖 + 硬件映射 overlay；CLI `--profile <名>`；同一套工程在 simulation / bench / CI 三个环境零修改切换。
5. ~~**DBC 变更自动检测**~~ ✅：运行中校验和轮询，外部修改自动刷新（消灭"改库忘重载"）；重载后为 Simulated 节点补建库里新增报文的生成器条目（不自动激活——"不发车"红线同样适用于重载）。
6. **格式收尾**：工程 JSON 加 `schema_version`；运行产物与本地状态明确 gitignore 边界。

### 明确不作

- 自由命名、与 DBC 无关的逻辑节点（上一版 GenNode 方案）作废：角色只能挂在 DBC 已有节点上，防"第二份节点清单"复活。
- 图形化拓扑编辑器：继续不做，实体表（见节点体系）足够。

## 节点体系（并入 P0 角色模型）

角色是节点的**角色**，节点还需要**驱动**——驱动形态继续可插拔：

- ~~GeneratorGroup 卡片~~ ✅ 已被**角色卡片**取代（P0 切片 1：全部 DBC 节点 + 三态角色）。
- **ReplayBlock 驱动**：把日志回放拆成可独立开关的回放块（按 id/发送节点过滤），作为一类节点驱动。
- **网络实体表（outliner）**：扁平实体表——节点不分类型文件夹，kind 用图标 + 徽标 + 行内开关 + 双击打开该节点自己的 UI + 右键插入菜单选类型；**纯 imgui 表格实现，不做图形化拓扑**。依赖角色模型先行。
- **测试模块**（远期）：带判定结果的自动化测试单元。
- 驱动与角色的关系：角色回答"这个节点现在谁扮演"，驱动回答"扮演时用什么跑"——一个节点同一时刻一个角色、一个驱动；切换角色不销毁驱动。

## 其他待办（并行小项）

### 观测与触发

- **State Tracker 收尾**：右键把 Data/Graphics 窗口里的信号直接加入跟踪；状态区段表的 CSV 导出口径细化。
- **触发数值常量 UI 化**（待议）：预触发 256 帧 / post-roll 32 帧 / 标记上限目前在 `bus.rs` 顶部常量。

### 结构

- **Trace 落盘**（#9）：`TRACE_LIMIT = 50_000` 固定环形缓冲，长时间抓取静默丢头部。方向：分段索引或磁盘支撑的 Trace；录制支持按过滤器只落一部分。
- **派生信号**（#11）：表达式求值（`a-b`、`(x>3)?1:0`、单位换算），派生信号能进 Graphics 与 Data。候选方案：复用脚本语言求值器。
- **双类展示层四元组化**：网络视图、TX 选择器、报文过滤器对同值双类只列一条（标准优先）。等有真实双类库需求再做。
- **动态库加载外部仿真元件**：进程内注册表已就绪（`script::register_extern`），动态库入口另议。
- **BLF→ASC 转存**：前置是改"回放启动丢弃录制状态"的产品语义。

### 需要人工验收（无 UI 自动化测试床）

- **总线核心线程化**：拖窗口、开模态、卡鼠标时波形不断流。
- **历史数据共享与性能**：多曲线 + Trace 1000 行 + 多窗口下，CPU 占用不高于单线程版。
- **v0.10.0 仿真节点全链**：Nodes 窗口、五个示例脚本、触发新动作（标记/清 Trace）、Graphics 标记竖线、State Tracker 双模式。

## 阶段 5：硬件源落地（适配器选型后启动）

适配器作为核心的又一个 `FrameSource`：RX 由驱动线程喂入，硬件时间戳锚定到核心时间轴；发生器 TX 由核心调度线程直接写适配器。硬件映射走 P0 的 Profile overlay。

- 分期：只收（2–4 天）→ 发送（约 1 周）→ CAN FD 与错误状态（数周）。
- 选型：CANable/candleLight（全 Rust 生态，无厂商 SDK 授权）或 PCAN-USB（PCanBasic.dll，行业最常见）。
- CI 替身：单通道 loopback 作为无硬件默认。

## 明确暂缓 / 边界外

- **网关与跨总线路由模型**：依赖多数据库与节点体系先行，另立项。
- **诊断层（UDS / CDD / ODX）**：按"更多协议"那一轮一起谈。
- **MF4 读取**：卡在"先拿到 Vector 真实导出样本"，没有样本不可验证。
- **LIN / FlexRay / 以太网**：按协议那一轮一起谈。

## 弯路记录（教训与纠正状态）

按影响排序。已纠正的保留条目作为回归防线；未纠正的指向对应待办。

1. **节点清单双源**（影响最大；纠正中 → P0 角色模型）：仿真节点名单存了两份——DBC 发送节点一份、`sim_nodes` 一份，"以谁为准"悬空。症状：节点仿真开关单向联动 TX 条目（反过来不联动）、Network 勾选与条目开关语义打架、改库后状态漂移。
2. **帧类硬编码假设**（✅ 已纠正）：`build_node_inputs` 硬编码 `extended: false`——扩展报文的 `sig()` 永远读不到值；脚本 `send` 长缓冲无校验——能发出无 FD 标志的 64 字节坏帧。纠正：帧类进键模型、FD 自动取整 + 长度上限（fe3c331 之前的 955dac5）。
3. **脚本内建裸奔**（✅ 已纠正）：`ramp`/`sine_wave` 从已清空的栈取参（一调即崩）、`srand` 栈失衡——都是实现先落地、测试后补时暴露的。教训：内建落地即带测试，不留"先跑通再说"。
4. **跨层契约回归**（✅ 已纠正）：extern 解析链改造时，把"钩子拒绝调用 = 报错"静默改成了 nil。教训：改解析链先写契约测试钉住两条路径各自的语义。
5. **提交不完整**（✅ 已纠正）：一次提交漏掉 bus.rs，历史提交点编译不过。教训：提交前全量 `cargo build --tests`，而不是只验证改动的文件。
6. **测试写 CWD**（✅ 已纠正，翻车两次）：录制类测试不设路径 → 默认派生 CWD `record_*.asc`，并行运行同秒同名互删文件；修正后又出现漏网（rearm 测试）。教训：每个新录制测试显式 temp 路径，全量测试跑完检查 `git status`。
7. **PowerShell 批量改文件**（✅ 教训已记）：批量重写文本引入 BOM 与多字节乱码。教训：批量文本修改只用 Edit 工具。
8. **裸 id 展示层的双类妥协**（已知妥协，未纠正）：选择器对同值双类只列一条、ImGui 控件 ID 需手工区分。等真实双类库需求。
9. **50k Trace 环静默丢帧**（未纠正 → 结构待办 #9）：长抓取丢头部。磁盘支撑方案见结构待办。

## 备注

- UI 无 imgui 自动化测试床：控件行为验收靠人工，判定逻辑用无头测试自动证明（详见 `docs/architecture.md` 开发约定）。
- 唤醒精度远期可选：事件驱动核心的"最后 <1 ms 自旋收尾"开关（详见 `docs/architecture.md` 核心时钟模型）。
