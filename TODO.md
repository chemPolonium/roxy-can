# TODO

能力补齐清单（不含硬件接口与其它总线协议，那两类另议）。按优先级排列：P0 是"现在就在给出错误结果"，P1 是"用已有数据就能换来的能力"，P2 是"决定能不能查偶发问题的结构"，P3 是"平台性的表达层"。

立单时间：2026-08-31，v0.6.0 之后。文中的 `file:line` 是当时的实际代码位置，动之前先复核。

## P0 解码正确性

1. **复用报文（mux）解码**
   现状：`SymbolTable` 只抄了 `name/start_bit/size/byte_order/value_type/factor/offset/min/max/unit/receivers`，没有任何 mux 字段（`src/dbc.rs:110-131`），解码是对匹配 ID 的**全部** `SG_` 无条件进行（`src/dbc.rs:202-214`）。挂一份带 `SG_ M` / `m12` 的库，Trace/Data/Graphics 会同时显示当前这一帧根本不存在的子报文信号。
   要做：`SignalInfo` 带 mux 指示，按 switch 值分组解码；`M`、`m12`、以及扩展复用（`extended_multiplex`，can-dbc 10 已暴露 `message_multiplexor_switch`）三档都要判；Trace / Messages / Data / Graphics 四条链路一致。
   验收：一份 mux 测试库，切到组 1 时组 2 的信号**不出现**（而不是显示 0）。

2. **VAL_ 值表**
   现状：`db.value_tables` 全程未解析。数据库写了 `0=OK 1=Error` 的地方，工具显示裸数字。
   要做：随符号表带值表，解码结果显示文本（CSV 导出同时给裸值与文本，不要只给一个）。

3. **标准帧与扩展帧共用裸 `u32` 做键**
   现状：聚合与 DBC 都只用裸 ID（0.6.0 明确接受并写进了 README），所以 `0x123` 标准帧与 `0x123` 扩展帧互相顶名，会假报 Unknown / Dlc、也会让两个观测器读到同一份聚合。
   要做：键改成 `(id, extended)`。这是有传染性的改动（`aggs`、`spec`、DBC 查表、导出、工程文件里的 ID 集合），先想清楚迁移再动。

4. **`SigType` / 单位的显示口径**
   现状：只提取了线性 `factor`/`offset`（`src/decode.rs:71-79`），`min`/`max` 只用于生成器编辑框夹范围，不显示。
   要做：至少把单位与信号类型显示到解码值后面；浮点/整型不要按同一格式打。

## P1 用已有聚合数据换来的视图

5. **总线负载 / 帧率-时间图 + 每总线错误帧计数**
   现状：`MessageAgg` 已有 `count` / `len` / 周期 EMA / `min_us` / `max_us`（`src/app.rs:2204-2243`），但没有"负载"这个视图。
   要做：**先定口径**——真实负载要按位时间加权（经典 CAN 与 FD 的位时间差一个数量级，FD 无 BRS 时 64 字节的位时间远超经典 8 字节），只按帧数算出来的东西会被当成错的。
   验收：同一段日志在 500 kbps 与 FD 2M 数据段下算出的负载不同，且与手算量级一致。

6. **规格监视出报告**
   现状：0.6.0 的违规表是唯一的 pass/fail 形状，但没有导出、没有测试用例概念、没有事件日志（`App::status` 是单槽、每帧覆写）。
   要做：违规表导出 CSV/HTML，并附上判定前提——用的哪份库、容差与宽限、有多少报文声明了周期。没有这些上下文，一份报告无法被第二次复核。

## P2 结构解锁

7. **回放时注入**
   现状：生成器被 `matches!(self.mode, Mode::Virtual)` 挡死（`src/app.rs:2156`），所以"回放真实日志 + 同时我发几条报文搅局"做不到；也不能合并两份日志或改时间戳。
   要做：取消这道门，并先决定两套时基谁说了算（建议以日志时基为主，生成帧落在同一 `sim_t_us` 上）。分析价值/成本比里最划算的一条。

8. **触发层**
   现状：代码里搜 `trigger` 只命中 ASC 头文本和"事件触发周期"的注释。没有条件启停录制、没有边沿/电平触发、没有 pre/post 缓冲、没有由总线事件打的标记（Graphics 的 `markers` 只是采样点圆点，见 `src/ui/graphics.rs:242`）。
   要做：条件（信号值越过阈值 / 某 ID 出现 / 周期超时 / 错误帧）→ 动作（开始·停止录制、插入标记、清 Trace），配 pre/post 环形缓冲。没有这个，复现偶发异常仍然靠运气。

9. **Trace 突破 50k 环 + 按过滤器落盘**
   现状：`TRACE_LIMIT = 50_000`（`src/app.rs:18`）固定环形缓冲，满了就丢头部（`src/app.rs:2190-2197`）——长时间实时抓取会**静默丢掉开头**；而录制走的是**每帧、不过滤**（`src/app.rs:2191-2193`）。方向正好相反的做法是：测量数据落盘、并且能只落被选中的那部分。
   要做：分段索引或磁盘支撑的 Trace；录制能按当前过滤器只落一部分。注意这会影响第 5 项的取数方式，所以第 5 项先做。

## P3 表达层

10. **反应规则 `on message X → send Y`**
    现状：`src/main.rs` 不解析任何命令行参数；无脚本、无规则引擎、无定时器回调；发送侧只有固定周期定时器 + 四种波形（`src/app.rs:2156-2184`、`src/sim.rs:11-24`）。
    要做：最小一块砖——某报文到达后发一条，载荷可引用触发帧的信号值。它与第 8 项共用同一个条件求值器，**不要分两次写**。

11. **信号数学 / 派生信号**
    现状：没有任何算术概念，全仓库搜不到 `expr` / `formula` / `compute` / `derived`；解码值就是 `raw * factor + offset`。
    要做：表达式求值（`a-b`、`(x>3)?1:0`、单位换算），派生信号能进 Graphics 与 Data。

12. **headless CLI**
    现状：只有 GUI 入口。
    要做：`--project` / `--replay` / `--export`，让上面这些能脱离界面在 CI 里跑。它排在最后，但它是第 10、11 项唯一的自动验收手段——真要自动化时它会自己往上冒。

## 明确暂缓 / 边界外

- **一总线多库合并、网关与跨总线路由模型**：`Channel.dbc` 是 `Option<SymbolTable>`（`src/app.rs:279-287`），一总线一库是硬假设，改动面比看起来大。
- **诊断层（UDS / CDD / ODX）**：会被"更多协议"这条边界挡住，按协议那一轮一起谈。
- **MF4 读取**：仍然卡在"先拿到 Vector 真实导出样本"，没有样本不可验证。
- **硬件后端（SocketCAN / PCAN / Kvaser / Vector XL）**与 **LIN / FlexRay / 以太网**：本清单不含。

## 依赖与取舍

- 第 8 项的"条件"与第 10 项的"规则"是同一个求值器，合并设计。
- 第 9 项会改变第 5 项的取数方式，所以第 5 项先落地更划算。
- 第 1、2、4 项都是解码层，适合作为一个"解码正确性"版本（0.7.0）一起做，README 里能说清"以前哪些库会看错"。
- 本工具界面无自动化测试床（`src/ui/*` 没有任何 imgui 测试），所有条目的验收都分两半：判定逻辑能自动证明，控件行为只能人眼看。
