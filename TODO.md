# TODO

只记录**尚未完成**的工作项。已完成能力的行为说明见 `README.md` 与 `docs/usage.md`；系统结构与设计决策见 `docs/architecture.md`；历史实现过程见 git log。

## 待办

### 观测与触发

- **State Tracker 收尾**：右键把 Data/Graphics 窗口里的信号直接加入跟踪；状态带内最小显示时长（抖动信号的碎带合并）；状态区段表的 CSV 导出口径。
- **触发数值常量 UI 化**（待议）：预触发 256 帧 / post-roll 32 帧 / 标记上限目前在 `bus.rs` 顶部常量。
- ~~触发条件锁存重置~~ ✅ 2026-09-07：`ClearTrace` 动作触发时同时重置所有出现类条件（ID 出现/错误帧）的锁存——"清一次、等下次"；Triggers 窗口新增 **Re-arm latched** 按钮手动重置。信号越阈与周期超时的电平是真实电平，永不受重置影响。

### 结构

- **Trace 落盘**（#9）：`TRACE_LIMIT = 50_000` 固定环形缓冲，长时间抓取静默丢头部。方向：分段索引或磁盘支撑的 Trace；录制支持按过滤器只落一部分。负载视图改从落盘数据取数即可，接口不变。
- **派生信号**（#11）：表达式求值（`a-b`、`(x>3)?1:0`、单位换算），派生信号能进 Graphics 与 Data。候选方案：复用脚本语言求值器。
- **脚本显式扩展帧语法**：数值 ≤ 0x7FF 的扩展帧（合法但不常见）无法被 `on message` 匹配，`send` 也永远按标准帧发。需语言设计决策（如 `on extended message 0x50` / `send_ext`）。
- **双类展示层四元组化**：网络视图、TX 选择器、报文过滤器对同值双类只列一条（标准优先）。等有真实双类库需求再做。
- **动态库加载外部仿真元件**：进程内注册表已就绪（`script::register_extern`），动态库入口另议。
- **BLF→ASC 转存**：前置是改"回放启动丢弃录制状态"的产品语义（录回放只会复刻日志）。

### 节点体系与多数据库（2026-09-07 立项）

要解决的问题：今天"脚本节点（Nodes 窗口）、TX 生成器条目、Network 视图按 DBC 节点模拟"是三种驱动形态的雏形，但分散在三个窗口、三套数据结构——用户看不出它们是同一类东西，也无法用同一种方式开关它们。目标：一条总线上的所有仿真源成为**一类可列举、可开关、可双击打开编辑的东西**；数据库与总线解耦，一条总线可挂多份 DBC 词汇表。设计思想参考过 CANoe 的 Simulation Setup，但不拘泥其形态：**使用优先**——能用的简单表格优于华丽的面板，每一步从现有窗口的真实痛点出发。

- **多数据库挂接**（原暂缓项"一总线多库"转正）：`Channel.dbc: Option<Arc<SymbolTable>>` → `Vec<Arc<SymbolTable>>`，解码/查名按库序尝试（先命中先用），重名报文按库序取先并记录归属库名；工程格式 `dbc_path` → `dbc_paths`（旧字段照载为首项，serde 缺省保旧工程）。UI：Buses 行改为数据库列表（加载/卸载/排序），Messages/Statistics/Data 标注信号归属库。牵连面：`SymbolTable` 查找已全部收口在 `message_of`/`messages`/`decode_signals` 几处，改动有界。
- **节点体系统一**：把"节点"提升为一等概念，`NodeKind::{Script, GeneratorGroup, ReplayBlock}`：
  - 第一阶段：**GeneratorGroup 节点卡片**——Network 视图已有的"Simulate this node"（按 DBC 发送节点聚合 TX 条目）提升为与 ScriptNode 同表呈现的节点卡片，同一开关语义、同一 NodeView 投影；
  - 第二阶段：**ReplayBlock 节点**——把日志回放拆成可独立开关的回放块（按 id/发送节点过滤），取代当前"整段日志回放 + 同 ID 静音"的粗粒度；
  - 每类节点保留自己的 UI（脚本编辑器 / 生成器条目表 / 回放块设置），对齐 CANoe"节点各有界面"。
- **Simulation / Real-time 节点模式**：现阶段节点开关即"仿真/停"；阶段 5 硬件落地后 Real-time 语义为**被动身份**（不代发流量，只提供该节点的数据库词汇与显示归属，真实流量直接过总线）。补一个对齐 CANoe 的便利项：**全部切仿真 / 全部切实时**总开关。
- **网络实体表（替代图形拓扑的远期项）**：总线拓扑图信息量低，采用简单的扁平实体表表示网络——节点不分类型文件夹，kind 用图标 + 徽标（`[脚本]`/`[生成器组]`/`[回放块]`）+ 行内仿真开关 + 双击打开该节点自己的 UI + 右键插入菜单选类型；数据库与信道收成一行附属项；顶部计数与筛选框。**使用优先：纯 imgui 表格/树实现，不做图形化拓扑**。依赖 NodeKind 统一先行（模型层就绪后这是纯视图工作）；现有 Network 视图够用就一直作过渡。
- **测试模块概念**（远期暂缓）：带判定结果的自动化测试单元（对应 CAPL Test Module / .NET Test Module），待节点体系稳定后设计。
- 依赖顺序：多数据库先行——节点按名字引用数据库词汇依赖"一条总线能挂多份词汇表"。

### 需要人工验收（无 UI 自动化测试床）

- **总线核心线程化**：拖窗口、开模态、卡鼠标时波形不断流。
- **历史数据共享与性能**：多曲线 + Trace 1000 行 + 多窗口下，CPU 占用不高于单线程版；backfill 阻塞核心线程的观察与后续挪移。
- **v0.10.0 仿真节点全链**：Nodes 窗口、五个示例脚本、触发新动作（标记/清 Trace）、Graphics 标记竖线、State Tracker 双模式。

## 阶段 5：硬件源落地（适配器选型后启动）

适配器作为核心的又一个 `FrameSource`：RX 由驱动线程喂入，硬件时间戳锚定到核心时间轴；发生器 TX 由核心调度线程直接写适配器；信道 UI 加适配器选择、连接状态、bus-off 恢复。

- 分期：只收（2–4 天）→ 发送（约 1 周）→ CAN FD 与错误状态（数周）。
- 选型：CANable/candleLight（全 Rust 生态，无厂商 SDK 授权）或 PCAN-USB（PCanBasic.dll，行业最常见）。

## 明确暂缓 / 边界外

- **网关与跨总线路由模型**：跨总线转发（含多总线时基裁决）依赖多数据库与节点体系先行，另立项。
- **诊断层（UDS / CDD / ODX）**：按"更多协议"那一轮一起谈。
- **MF4 读取**：卡在"先拿到 Vector 真实导出样本"，没有样本不可验证。
- **LIN / FlexRay / 以太网**：按协议那一轮一起谈。

## 备注

- UI 无 imgui 自动化测试床：控件行为验收靠人工，判定逻辑用无头测试自动证明（详见 `docs/architecture.md` 开发约定）。
- 唤醒精度远期可选：事件驱动核心的"最后 <1 ms 自旋收尾"开关（详见 `docs/architecture.md` 核心时钟模型）。
